let velocidade = 0

do {
    alert("A velocidade do veículo é " + velocidade + " Km/h")
    velocidade -= 20
} while (velocidade > 0) 

alert("Velocidade: " + velocidade + "km/h")

// 

let dia = 1;

do {
    console.log("Dia", dia);
    dia++
} while (dia <= 30);